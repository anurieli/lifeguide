# Roadmap

**Status:** ✅ current. Update with `../CHANGELOG.md` whenever phasing changes.

## v1 — the foundation + the two surfaces (one roof, one shared context)
Built foundation-first; surfaces are thin plugins on the context spine.
- **Plan 1 — Foundation + Whiteboard** (✅ written): Next.js + Convex + auth, schema, node/edge model, canvas, capture→distill→place, context scaffolding. → `plans/2026-05-20-lifeguide-v1-plan1-foundation-whiteboard.md`
- **Plan 2 — The Coach:** multi-turn agent loop, tool registry, the context-assembler consumer, AI manipulation across surfaces, Mirror compaction.
- **Plan 3 — Audio → nodes:** record → Whisper → segment → place.
- **Plan 4 — Guide + Pillars + Settings + Daily ritual:** the text-layer surface, pillar picker, settings (daily exercise / alerts / treatment), morning & evening bookends.

## v1.5 — the tether and the loop begins
Off-platform Coach (SMS/push), the reflection loop (act → did it work → revise), resurfacing engine, North Star ceremony (auto-trigger), voice memos, browser extension.

## v2 — productivity + depth
Calendar/to-do integration (context → then active), the full alignment engine, the Vision Board surface, time-binding (nested horizons), people as first-class objects, drift visualization, native apps, multi-board.

## Later
Coach Studio / pluggable Playbooks, multi-tenant cloud MCP (agent platform), print & ship.

> Out forever: streaks/gamification, social feed, generic blank-page journaling, productivity-app framing.
